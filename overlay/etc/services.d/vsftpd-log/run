#!/usr/bin/with-contenv sh

set -euo pipefail
export BCG_E="${PWD##*/}/$(basename ${0})"


#======================================================================================================================
# Forward vsftpd error log to Docker.
#======================================================================================================================

bcg-forward ${PWD##*/} ${FTPS_LOG_FILE}
